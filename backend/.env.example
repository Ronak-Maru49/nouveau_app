MONGODB_URI=mongodb://localhost:27017/nouveau_app
PORT=5000

# Optional: set these to send order bills automatically from the backend.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
